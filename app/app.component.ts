import { Component } from '@angular/core';

@Component({
    selector: 'my-app',
    template:`
        <navigation-bar></navigation-bar>
    `
})
export class AppComponent {
    title = 'Index-page!';
}
